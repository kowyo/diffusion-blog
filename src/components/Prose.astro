<div class="prose">
  <slot />
</div>

<style>
  .prose {
    font-family: var(--font-body);
    font-size: 1.125rem; /* Ensure prose text stays 18px */
    line-height: 1.75;
    color: var(--color-text);
  }

  .prose :global(p) {
    margin-bottom: 1rem; /* mb-4 */
  }

  .prose :global(h1) {
    font-family: var(--font-heading);
    font-size: 2.25rem;
    font-weight: 400;
    letter-spacing: -0.02em;
    line-height: 1.2;
    margin-top: 3rem; /* mt-12 */
    margin-bottom: 0.5em;
    color: var(--color-text);
  }

  @media (max-width: 640px) {
    .prose :global(h1) {
      font-size: 1.25rem;
    }
  }

  .prose :global(h2) {
    font-family: var(--font-heading);
    font-size: 1.375rem;
    font-weight: 400;
    letter-spacing: -0.02em;
    line-height: 1.3;
    margin-top: 3rem; /* mt-12 */
    margin-bottom: 0.5em;
    color: var(--color-text);
  }

  .prose :global(h3) {
    font-family: var(--font-heading);
    font-size: 1.125rem;
    font-weight: 400;
    letter-spacing: -0.01em;
    line-height: 1.4;
    margin-top: 3rem; /* mt-12 */
    margin-bottom: 0.5em;
    color: var(--color-text);
  }

  /* Reduce gap when a heading immediately follows another */
  .prose :global(h1 + h2),
  .prose :global(h2 + h3),
  .prose :global(h1 + h3),
  .prose :global(h1 + h1) {
    margin-top: 0.5em;
  }

  /* Remove top margin for the first element in the content */
  .prose :global(> :first-child) {
    margin-top: 0;
  }

  .prose :global(a) {
    color: var(--color-link);
    text-decoration-thickness: 1px;
    text-underline-offset: 2px;
  }

  .prose :global(a:hover) {
    color: var(--color-link-hover);
  }

  .prose :global(blockquote) {
    border-left: 2px solid var(--color-border);
    padding-left: var(--space-lg);
    margin: 1.5em 0;
    color: var(--color-text-secondary);
    font-style: italic;
  }

  .prose :global(ul),
  .prose :global(ol) {
    margin-bottom: 1.5em;
    padding-left: 1.5em;
  }

  .prose :global(li) {
    margin-bottom: 0.375em;
  }

  .prose :global(code) {
    font-family: var(--font-mono);
    font-size: 0.85em;
    background-color: var(--color-code-bg);
    padding: 0.125em 0.375em;
    border-radius: 3px;
  }

  .prose :global(pre) {
    background-color: var(--color-code-bg);
    padding: var(--space-lg);
    border-radius: 4px;
    overflow-x: auto;
    margin: 1.5em 0;
    line-height: 1.5;
  }

  .prose :global(pre code) {
    background: none;
    padding: 0;
    font-size: 0.8125rem;
  }

  .prose :global(hr) {
    border: none;
    border-top: 1px solid var(--color-border);
    margin: 2.5em 0;
  }

  .prose :global(img) {
    border-radius: 4px;
    margin: 1rem 0; /* Reduced from 1.5em */
  }

  .prose :global(strong) {
    font-weight: 600;
  }

  .prose :global(.katex-display) {
    overflow-x: auto;
    overflow-y: hidden;
    padding: 0.25rem 0;
    margin: 1rem 0;
  }

  .prose :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 1.5em 0;
    font-size: 0.9375rem;
    display: block;
    overflow-x: auto;
  }

  .prose :global(thead) {
    border-bottom: 2px solid var(--color-border);
  }

  .prose :global(th) {
    text-align: left;
    font-weight: 600;
    padding: 0.5em 1em;
    color: var(--color-text);
  }

  .prose :global(td) {
    padding: 0.5em 1em;
    border-bottom: 1px solid var(--color-border);
    vertical-align: top;
  }

  .prose :global(tr:last-child td) {
    border-bottom: none;
  }
</style>
